apiVersion: v1
kind: Service

metadata:
  name: microcoders-redis
  namespace: microcoders

spec:
  selector:
    app: microcoders-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
